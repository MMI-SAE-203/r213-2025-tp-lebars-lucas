<div x-data="{
	firstName: 'John',
	lastName: 'Doe',
	fullName() {
	return `${this.firstName} ${this.lastName}`
	}
	}">
	<p>Nom complet : <span x-text="fullName"></span></p>
	</div>

	<div x-data="{ count: 0 }">
		<button x-on:click="count++">Augmenter</button>
			<button x-on:click="count--">Baisser</button>
		 <span x-text="count"></span> fois
		</button>
		</div>

		

		<div x-data="{
			isLoggedIn: false,
			username: 'Utilisateur'
			}">
			<div x-show="!isLoggedIn">
			<button class="bg-teal-200 rounded-2xl p-3" x-on:click="isLoggedIn = true">
			Se connecter
			</button>
			</div>
			<div x-show="isLoggedIn">
			Bienvenue, <span x-text="username"></span> !
			<button class ="bg-teal-200 rounded-2xl p-3" x-on:click="isLoggedIn = false">
			Se déconnecter
			</button>
			</div>
			<template x-if="isLoggedIn">
			<div>Contenu réservé aux utilisateurs connectés
			</div>
			</template>
			</div>



			<div x-data="{textColor:'', changeColor : true}">
		<p x-bind:class="changeColor ? textColor: ''">Ce texte change de couleur</p>	
<button x-on:click="textColor='text-red-400'">Rouge</button>
<button x-on:click="textColor='text-green-400'">Vert</button>
<button x-on:click="textColor='text-blue-400'">Bleu</button>
</div>

	<div x-data="{
		Prénom: '',
		Nom: '',
		Nomcomplet() {
		return `${this.Prénom} ${this.Nom} `
}
}
">
		<p>Nom complet : <span x-text="Nomcomplet"></span></p>
	
		<input x-model="Prénom" type="text" placeholder="Prénom">
		<input x-model="Nom" type="text" placeholder="Nom">
		
		</div>
		<div x-data="{ open: false }">
			<button @click="open = ! open">Toggle</button>
		 
			<div x-show="open" x-transition>
				Hello 👋
			</div>
		</div>