import { allMaisons, oneID, allMaisonsFavori, allMaisonsSorted, bySurface, surfaceORprice, getAgentByID } from './backend.mjs';


/*try {
    const records = await allMaisons() ;
    console.log(JSON.stringify(records, null, 2)) ;
    } catch (e) {
    console.error(e) ;
    }

    try {
        const records = await oneID('n03d3dpwit1wxz5') ;
        console.log(JSON.stringify(records, null, 2)) ;
        } catch (e) {
        console.error(e) ;
        }

        try {
            const records = await allMaisonsFavori() ;
            console.log(JSON.stringify(records, null, 2)) ;
            } catch (e) {
            console.error(e) ;
            }

            try {
                const records = await allMaisonsSorted() ;
                console.log(JSON.stringify(records, null, 2)) ;
                } catch (e) {
                console.error(e) ;
                }
                try {
                    const records = await bySurface(200) ;
                    console.log(JSON.stringify(records, null, 2)) ;
                    } catch (e) {
                    console.error(e) ;
                    }


                    try {
                        const records = await surfaceORprice(1001,299) ;
                        console.log(JSON.stringify(records, null, 2)) ;
                        } catch (e) {
                        console.error(e) ;
                        }*/

                        try {
                            const records = await getAgentByID('22gqyck5b8k550u') ;
                            console.log(JSON.stringify(records, null, 2)) ;
                            } catch (e) {
                            console.error(e) ;
                            }
    

    

